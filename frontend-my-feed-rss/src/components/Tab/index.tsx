import { StyledTab, StyledButton } from './styles'

export default function Tab() {
  return (
    <StyledTab>
      <StyledButton>Publicações</StyledButton>
      <StyledButton>Membros</StyledButton>
    </StyledTab>
  )
}
